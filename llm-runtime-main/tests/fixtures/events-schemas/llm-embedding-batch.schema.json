{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "llm_embedding_batch",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "eventType",
    "callId",
    "provider",
    "model",
    "timestamp",
    "batchIndex",
    "batchTextsCount",
    "durationMs"
  ],
  "properties": {
    "eventType": { "const": "llm_embedding_batch" },
    "callId": { "type": "string", "minLength": 26, "maxLength": 26 },
    "provider": {
      "enum": [
        "anthropic",
        "openai",
        "google",
        "deepseek",
        "mistral",
        "groq",
        "together",
        "ollama"
      ]
    },
    "model": { "type": "string" },
    "timestamp": { "type": "string", "format": "date-time" },
    "batchIndex": { "type": "integer", "minimum": 0 },
    "batchTextsCount": { "type": "integer", "minimum": 0 },
    "durationMs": { "type": "integer", "minimum": 0 }
  }
}
